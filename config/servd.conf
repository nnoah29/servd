# servd configuration file
# Lines starting with # are ignored.
# Format: key = value

# --- Listen ports ---
tcp = 8080
tcp = 9090
udp = 8081
unix = /tmp/servd.sock

# --- Logger ---
# Niveaux: DEBUG, INFO, WARN, ERROR
log_level = INFO
# log_file = /var/log/servd.log
